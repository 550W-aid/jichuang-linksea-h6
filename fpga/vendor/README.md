# 厂商相关文件归档说明

这个目录只放和厂商工具链、板卡平台强相关的内容，避免污染可复用 RTL。

建议结构：

```text
fpga/vendor/
|-- pll/       厂商 PLL 工程或生成文件
|-- project/   eLinx 工程模板、器件库、工程配置
`-- sdram/     SDRAM 控制器、RAM IP、相关约束或示例
```

接入建议：

- 先把 `UART + VGA + LED` 相关约束和工程跑通，再逐步接入这里的内容。
- 如果某些文件受许可证限制，不方便纳入源码压缩包，至少在交付文档中写清获取方式。

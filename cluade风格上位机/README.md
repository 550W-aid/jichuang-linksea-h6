# cluade风格上位机

这个目录只存放上位机代码。

来源：

- 本地源目录：`C:\Users\Fangr\OneDrive\Desktop\ccic_host_gui`

当前包含：

1. `main.py`
   上位机入口。
2. `app/`
   主界面、协议、样式和后台 worker。
3. `requirements.txt`
   运行依赖。
4. `run_gui.bat` / `run_gui.ps1`
   启动脚本。

后续约定：

1. 以后上位机相关改动继续直接更新这个目录。
2. 不提交 `.venv`、`__pycache__`、备份目录和其他本地生成文件。
